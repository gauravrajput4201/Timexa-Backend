# Application Configuration
NODE_ENV=production
PORT=3000

# MongoDB Configuration (Production)
# Replace with your production MongoDB URI (e.g., MongoDB Atlas)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/timexa

# CORS Configuration
# Set to your frontend domain in production
CORS_ORIGIN=*
